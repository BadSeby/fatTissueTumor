% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/preprocess_pipeline.R
\name{preprocess_expression}
\alias{preprocess_expression}
\title{Advanced preprocessing pipeline for expression data}
\usage{
preprocess_expression(
  expr_matrix,
  min_expr = 1,
  min_expr_frac = 0.2,
  min_var = 0.01,
  max_na_frac = 0.2,
  min_expr_sample = 1,
  min_var_sample = 0,
  max_na_frac_sample = 0.2,
  normalize = "zscore",
  log_transform = FALSE,
  batch = NULL,
  return_report = FALSE
)
}
\arguments{
\item{expr_matrix}{Expression matrix (genes x samples).}

\item{min_expr}{Minimum average expression value to hold a gene (default: 1).}

\item{min_expr_frac}{Minimum fraction of samples in which gene must exceed min_expr (default: 0.2).}

\item{min_var}{Minimum variance threshold to hold a gene (default: 0.01).}

\item{max_na_frac}{Maximum fraction of NA allowed per gene (default: 0.2).}

\item{min_expr_sample}{Minimum mean expression value to hold a sample (default: 1).}

\item{min_var_sample}{Minimum variance threshold to hold a sample (default: 0).}

\item{max_na_frac_sample}{Maximum fraction of NA allowed per sample (default: 0.2).}

\item{normalize}{Normalization method: 'none’, ‘zscore’, ‘quantile’, ‘minmax’ (default: ‘zscore’).}

\item{log_transform}{If TRUE, apply log2(x+1) (default: FALSE).}

\item{batch}{Batch vector for batch correction (optional).}

\item{return_report}{If TRUE, also returns a detailed report (default: FALSE).}
}
\value{
Preprocessed matrix (and, if required, a report).
}
\description{
Performs sequentially: missing removal, filters on genes and samples, normalization, log-transform, batch correction.
}
\examples{
\dontrun{
data(expr_breast)
res <- preprocess_expression(expr_breast, log_transform = TRUE, return_report = TRUE)
str(res)
}
}
