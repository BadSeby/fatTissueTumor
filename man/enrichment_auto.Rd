% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/enrichment_wrapper.R
\name{enrichment_auto}
\alias{enrichment_auto}
\title{Universal enrichment wrapper: automatic ID conversion}
\usage{
enrichment_auto(gene_list, id_type = "auto", platform = NULL, ...)
}
\arguments{
\item{gene_list}{Vector of gene IDs (probe, symbols, Entrez, Ensembl).}

\item{id_type}{Type of ID if known ("auto", "symbol", "entrez", "ensembl", "probe"). Default: 'auto.}

\item{platform}{Platform name (e.g., "hgu133a", "hgu133plus2") if probe. Default: NULL.}

\item{...}{Other parameters passed to enrichment_analysis().}
}
\value{
object enrichResult.
}
\description{
Accepts a list of genes (probeID, symbols, Entrez, Ensembl) and automatically converts to HGNC symbols.
Then runs enrichment_analysis().
}
\examples{
if (requireNamespace("clusterProfiler", quietly = TRUE) &&
    requireNamespace("org.Hs.eg.db", quietly = TRUE)) {
  # Example with gene symbols (no internet required)
  gene_list <- c("TP53", "BRCA1", "EGFR", "MYC", "PTEN")
  # Automatic ID detection and enrichment (will use symbols)
  ego <- enrichment_auto(gene_list)
  if (methods::is(ego, "enrichResult") && nrow(ego@result) > 0) {
    print(head(ego@result))
  }
}
}
